import {
  StyleSheet, View
} from 'react-native'

// 用於 App 底部, 顯示更新成功...etc, 會自動關閉的小提示
export default function Hint(props) {
  return (
    <View>

    </View>
  )
}

const styles = StyleSheet.create({

})
